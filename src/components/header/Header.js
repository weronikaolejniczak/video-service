import React from 'react';
import './style.css';

const Header = () => {
    return (
        <header>
            <div className="logo">
                <span className="site-name">YTServe</span>
            </div>
        </header>
    );
}

export default Header;
